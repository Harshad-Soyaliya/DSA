#include<bits/stdc++.h>
using namespace std ;

int main () {

    // this is comment
    // int : store integer 
    int x = 50;

    // long : same as int but here range is bigger
    long y = 5000000;

    // long long : same as long but here also bigger range compare to long
    long long z = 10000000000000000;

    // float , double : for floating number store , and same as range difference

    float A = 50.22 ;
    double B = 100.005150 ;

    // string : store characters (string)

    // string : only store wold it means if after wold space so it not get 
    // ex : input : hey harshad , so it sotre only hey 

    string name = "hey harhad";
    cout << name <<endl <<endl;

    // get line is collect full single line , same as sting
    string fullname ;
    cout << "enter full name : " << endl ;
    getline(cin,fullname);
    cout << fullname <<endl <<endl;


    //char : store for single chracter
    char H = 'H';
    cout << H <<endl;


    return 0;
}